#include <stdio.h>

int main() {
int a,b;
printf("write the numbers you want to perform operations on");
scanf("%d%d",&a,&b);
printf("the sum of the numbers is %d\n", a+b);
printf("the product of the numbers is %d\n",a*b);
printf("the difference of the numbers is %d\n",a-b);
printf("the quoitent is %d",a%b);
return 0;
}
