#include <stdio.h>

int main() {
    int a,b;
    printf("write the breath and length of the rectangle");
    scanf("%d%d",&a,&b);
    printf("the area of the rectangle is %d\n",a*b);
    printf("the perimeter of the rectangle is %d\n",2*(a+b));

return 0;

}
